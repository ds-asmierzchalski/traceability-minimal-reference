apiVersion: v1
kind: Service
metadata:
  name: {{ include "cx-0125-traceability.fullname" . }}
  labels:
    {{- include "cx-0125-traceability.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cx-0125-traceability.selectorLabels" . | nindent 4 }}
