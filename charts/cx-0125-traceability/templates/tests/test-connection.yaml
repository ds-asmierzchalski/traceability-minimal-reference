apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cx-0125-traceability.fullname" . }}-test-connection"
  labels:
    {{- include "cx-0125-traceability.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cx-0125-traceability.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
